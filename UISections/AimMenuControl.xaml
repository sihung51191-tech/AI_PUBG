<UserControl x:Class="Aimmy2.Controls.AimMenuControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <UserControl.Resources>
        <Style x:Key="SectionFrame" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource ThemeSurfaceRaised}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource ThemeOutline}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
        </Style>
    </UserControl.Resources>
    <ScrollViewer x:Name="AimMenu"
                  VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Disabled"
                  PanningMode="VerticalOnly">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <StackPanel x:Name="ASP1"
                        Grid.Column="0"
                        Margin="10,0,6,0">
                <Border Style="{StaticResource SectionFrame}"><StackPanel x:Name="AimAssist" /></Border>
                <Border Style="{StaticResource SectionFrame}"><StackPanel x:Name="Predictions" /></Border>
                <Border Style="{StaticResource SectionFrame}"><StackPanel x:Name="TriggerBot" /></Border>
                <Border Style="{StaticResource SectionFrame}"><StackPanel x:Name="ESPConfig" /></Border>
                <Border Style="{StaticResource SectionFrame}"><StackPanel x:Name="LootConfig" /></Border>
            </StackPanel>
            <StackPanel x:Name="ASP2"
                        Grid.Column="1"
                        Margin="6,0,10,0">
                <Border Style="{StaticResource SectionFrame}"><StackPanel x:Name="AimConfigSlot1" /></Border>
                <Border Style="{StaticResource SectionFrame}"><StackPanel x:Name="AimConfig" /></Border>
                <Border Style="{StaticResource SectionFrame}"><StackPanel x:Name="FOVConfig" /></Border>
                <StackPanel x:Name="WeaponSlotSystem">
                    <Border Style="{StaticResource SectionFrame}"><StackPanel x:Name="WeaponRecognitionSystem"/></Border>
                    <Border Style="{StaticResource SectionFrame}"><StackPanel x:Name="ScopeRecognitionSystem"/></Border>
                </StackPanel>
                <Border Style="{StaticResource SectionFrame}"><StackPanel x:Name="RecoilConfig" /></Border>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</UserControl>
