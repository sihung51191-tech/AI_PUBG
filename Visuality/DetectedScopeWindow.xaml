<Window x:Class="Visuality.DetectedScopeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Detected Scope" SizeToContent="WidthAndHeight"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ShowActivated="False"
        ResizeMode="NoResize" IsHitTestVisible="False">
    <Border x:Name="OverlayRoot" Width="260"
            Background="{DynamicResource ThemeSurfaceRaised}" BorderBrush="{DynamicResource ThemeOutline}"
            BorderThickness="1" CornerRadius="10" Padding="9,7">
        <StackPanel>
            <Border Background="{DynamicResource ThemeSurfaceElevated}" CornerRadius="7" Padding="7,4" Margin="0,0,0,5">
                <TextBlock Text="SÚNG + SCOPE" Foreground="{DynamicResource ThemeTextPrimary}" FontWeight="SemiBold" FontSize="10.5"/>
            </Border>
            <Grid Margin="1,1">
                <Grid.ColumnDefinitions><ColumnDefinition Width="23"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <TextBlock Text="S1" Foreground="{DynamicResource ThemeTextSecondary}" FontSize="9.5" VerticalAlignment="Center"/>
                <TextBlock x:Name="Slot1WeaponText" Grid.Column="1" Text="Unknown" Foreground="{DynamicResource ThemeTextPrimary}" FontSize="10.5" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/>
                <Border Grid.Column="2" Background="{DynamicResource ThemePrimaryAction}" CornerRadius="5" Padding="6,2" Margin="6,0,0,0"><TextBlock x:Name="Slot1Text" Text="Không" Foreground="{DynamicResource ThemePrimaryActionForeground}" FontSize="9.5" FontWeight="SemiBold"/></Border>
            </Grid>
            <TextBlock x:Name="Slot1DetailText" Text="—" Foreground="{DynamicResource ThemeTextSecondary}" FontSize="9" Margin="24,0,0,4" TextTrimming="CharacterEllipsis"/>
            <Grid Margin="1,1">
                <Grid.ColumnDefinitions><ColumnDefinition Width="23"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <TextBlock Text="S2" Foreground="{DynamicResource ThemeTextSecondary}" FontSize="9.5" VerticalAlignment="Center"/>
                <TextBlock x:Name="Slot2WeaponText" Grid.Column="1" Text="Unknown" Foreground="{DynamicResource ThemeTextPrimary}" FontSize="10.5" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/>
                <Border Grid.Column="2" Background="{DynamicResource ThemeSecondaryAction}" CornerRadius="5" Padding="6,2" Margin="6,0,0,0"><TextBlock x:Name="Slot2Text" Text="Không" Foreground="{DynamicResource ThemeSecondaryActionForeground}" FontSize="9.5" FontWeight="SemiBold"/></Border>
            </Grid>
            <TextBlock x:Name="Slot2DetailText" Text="—" Foreground="{DynamicResource ThemeTextSecondary}" FontSize="9" Margin="24,0,0,5" TextTrimming="CharacterEllipsis"/>
            <Border Background="{DynamicResource ThemeSurface}" BorderBrush="{DynamicResource ThemeOutline}" BorderThickness="1" CornerRadius="7" Padding="7,5">
                <Grid>
                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock x:Name="ActiveSlotText" Text="Đang dùng S1" Foreground="{DynamicResource ThemeTextPrimary}" FontSize="10" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                        <TextBlock x:Name="ActiveProfileText" Text="Legacy scope profile" Foreground="{DynamicResource ThemeTextSecondary}" FontSize="9" Margin="0,1,0,0" TextTrimming="CharacterEllipsis"/>
                    </StackPanel>
                    <Border Grid.Column="1" Background="{DynamicResource ThemeTertiaryAction}" CornerRadius="5" Padding="6,3" Margin="6,0,0,0" VerticalAlignment="Center"><TextBlock x:Name="RecoilAdjText" Text="±0" Foreground="{DynamicResource ThemeTertiaryActionForeground}" FontSize="9.5" FontWeight="Bold"/></Border>
                </Grid>
            </Border>
        </StackPanel>
    </Border>
</Window>
